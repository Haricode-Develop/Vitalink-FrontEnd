// Estilos actualizados
export const modalStyle = {
    display: 'flex',
    flexDirection: 'row',
    // Ajuste del tamaño del modal. Por ejemplo, aumentar a 80% del viewport
    width: '80vw', // Antes era '100vw'
    height: '80vh', // Antes era '100vh'
    alignItems: 'center',
    justifyContent: 'center',
};

export const leftSection = {
    // Ajuste del ancho, por ejemplo, puede ser necesario reducir si el modal es más grande
    width: '60%', // Antes era '75%'
    padding: '20px',
    overflowY: 'auto', // Para scroll si hay muchos ejercicios
};

export const rightSection = {
    // Ajuste del ancho, por ejemplo, puede ser necesario aumentar si el modal es más grande
    width: '40%', // Antes era '25%'
    backgroundColor: '#f0f0f0', // Gris claro
    padding: '20px',
};

export const searchBar = {
    marginBottom: '20px',
};

export const exerciseItem = {
    // Estilos para cada ítem de ejercicio si es necesario
};
